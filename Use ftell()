#include <stdio.h>

int main()
{
    FILE *fp;

    fp = fopen("sample.txt", "r");

    if(fp == NULL)
    {
        printf("File not found.");
        return 1;
    }

    fgetc(fp);
    fgetc(fp);

    printf("Current Position = %ld", ftell(fp));

    fclose(fp);

    return 0;
}
Sample Output
Current Position = 2
